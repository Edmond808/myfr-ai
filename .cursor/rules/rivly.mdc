---
description: myfr.ai project conventions
globs:
alwaysApply: true
---

# myfr.ai conventions

- Brand name: **myfr.ai** (`BRAND` in `lib/constants.ts`, i18n `brand` field)
- Next.js App Router — no Vite, no client-side API keys
- Classification prompt in `lib/classify.ts` is core IP — do not change without review
- 8 categories must stay in sync: `lib/types.ts`, `schema.sql` enum, classifier prompt
- Anonymous classify OK; auth required at dispatch (see MIGRATION_PLAN.md Phase 2)
- i18n: add strings to both `lib/i18n/messages/en.ts` and `fr.ts`
- Merchant verification stays manual for first 50 pros
- Claude team workspace: `/Users/e808m/rivly-claude`
